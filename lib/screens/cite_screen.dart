import 'package:flutter/material.dart';

class CiteScreen extends StatelessWidget {
  const CiteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Cite Screen'),
      ),
    );
  }
}
